import { AbstractControlValueAccessorDirective } from './abstract-control-value-accessor.directive';

describe('AbstractControlValueAccessorDirective', () => {
  it('should create an instance', () => {
    const directive = new AbstractControlValueAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
